# Copy this file to .env and fill in the values.
# API configuration
# Base URL only — do NOT include the `/api` path; every repository already
# prefixes its routes with `/api/...`.
API_BASE_URL=http://160-153-179-249.sslip.io
CONNECT_TIMEOUT=35000
RECEIVE_TIMEOUT=35000
