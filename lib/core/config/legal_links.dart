/// Public URLs for the legal documents shown in-app (see
/// `InAppWebViewPage`). Kept in one place so the login consent copy and any
/// future settings entry point can't drift apart.
class LegalLinks {
  LegalLinks._();

  static const String privacyPolicy = 'https://vigil-1.com/child-privacy-policy';
  static const String termsAndConditions = 'https://vigil-1.com/terms-conditions';
}
